let a = 20;
let b = 5;

function operaçoes() {
  let soma = a + b;
  let subtração = a - b;
  let multiplicação = a * b;
  let divisão = a / b;
  let resto = a % b;
  console.log(
    soma,
    ", " + subtração,
    ", " + multiplicação,
    ", " + divisão,
    ", " + resto
  );
}
operaçoes();
